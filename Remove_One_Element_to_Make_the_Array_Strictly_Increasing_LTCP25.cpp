#include<iostream>
#include<vector>
#include<bits/stdc++.h>
using namespace std;

bool canBeIncreasing(vector<int>& nums) {
    int n = nums.size();
    int Satisfies = 0;

    for(int i = 1; i < n; i++) {
        
    }
    return false;
}

int main() {
    vector<int> arr{2,3,1,2};
    cout << canBeIncreasing(arr);
}